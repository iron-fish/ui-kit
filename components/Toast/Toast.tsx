import { Alignment, ConditionalToastProps } from './types'

export const Toast = ({
  alignment,
  visible,
  message,
  action,
  actionLabel,
}: ConditionalToastProps) => {
  // 
  return null;
}

export default Toast
